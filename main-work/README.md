# Main Work

Main app code lives here.

## Folders

- `app` - Expo React Native app (TypeScript)
- `mcp-server` - local MCP server (TypeScript)

## Start

App:

- `cd app`
- `npm start`

MCP server:

- `cd mcp-server`
- `npm run dev`
